<section class="card">
  <a href="/products/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/assets/{{filename}}" alt="{{description}}">
      <p>Product Description: {{description}}</p>
    </div>
  </a>
</section>